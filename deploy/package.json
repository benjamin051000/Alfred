{
  "name": "deploy",
  "version": "1.0.0",
  "description": "Deployment server for Alfred production via a webhook.",
  "main": "deploy-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Benjamin Wheeler",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
